# 急救方案：Trading 篇 — 代码与设计差异修订计划

**日期**: 2026-02-27
**依据**: `01-trading-pipeline代码与设计差异清单.md` (16项差异) + `02-backtest-pipeline代码与设计差异清单.md` (10项差异)

---

## 一、根本问题诊断

通过完整审计，发现问题的根源是一个**三角脱节**：

```
      设计文档（V3.3）
       /          \
      /  严重偏离  \
     /              \
Trading 代码 ←不一致→ Backtest 代码
```

1. **设计文档** 在 v3.3 版本定义了完整的 OOP 架构、数据模型、风控规则
2. **Trading 代码** 用 Pipeline 模式实现了极简版本，大量设计要求未落地
3. **Backtest 代码** 也用 Pipeline 模式，但独立实现了一些 Trading 设计中的高级功能（成交模型、冲击成本等），又缺失了另一些（总仓位上限）
4. **trading-api.md v4.0.0** 已经部分修正（引入 Pipeline 模式说明），但其他3个设计文档未同步

---

## 二、处理原则（关键决策点）

在制定急救方案之前，需要先确定一个核心方向:

### 方向A：文档对齐代码（Design follows Code）
- 承认当前 Pipeline 模式为正式架构
- 将设计文档降级/重写为反映实际代码行为
- 代码中不存在的功能（v2.0验证、regime阈值等）标记为 P1/P2 未来规划
- **优点**: 快速达成一致，设计与代码立即同步
- **缺点**: 放弃设计中的风控深度，系统风险敞口增大

### 方向B：代码对齐设计（Code follows Design）
- 以设计文档为权威标准
- 逐步将缺失的功能补入 Trading/Backtest 代码
- **优点**: 保持设计完整性和风控严谨性
- **缺点**: 工程量大，需要大量代码改动

### 方向C：双向收敛（推荐）
- **文档侧**: 更新实现状态、吸收代码中合理的"超额实现"（如一字板检测、风控事件表）
- **代码侧**: 补齐 P0 级缺失（关键风控、成交模型对齐）
- **共同侧**: 统一信号过滤逻辑、费用计算基准
- **优点**: 平衡工程量与系统安全
- **缺点**: 需要精确判断每一项"谁对齐谁"

---

## 三、分级急救任务

### 🔴 第一批 P0（紧急 — 影响系统正确性）

#### P0-1: Trading 成交模型对齐 Backtest
**问题**: Trading 用全额成交，Backtest 用部分成交，两者行为不一致
**方案**: 将 Backtest 的 `_estimate_fill()` 和 `_estimate_impact_cost()` 抽取为共享模块，Trading 复用
**修改文件**:
- 新增: `src/shared/execution_model.py`（从 backtest/pipeline.py 提取）
- 改动: `src/trading/pipeline.py` — `try_execute_buy()` 增加 fill_probability/fill_ratio/impact_cost 逻辑
- 改动: `src/backtest/pipeline.py` — 改为导入共享模块

#### P0-2: 信号过滤逻辑统一
**问题**: Trading 用 strict+fallback，Backtest 用 STRONG_BUY/BUY 白名单，设计用黑名单
**方案**: 统一为设计的黑名单方案 + 代码中已验证的额外保护
**统一过滤规则**:
```
1. final_score >= min_final_score (55)
2. recommendation not in {AVOID, SELL}
3. risk_reward_ratio >= 1.0
4. direction 映射: bullish→buy, bearish→sell, neutral→过滤
```
**修改文件**: `src/trading/pipeline.py`, `src/backtest/pipeline.py`
**决策点**: `opportunity_grade == "D"` 过滤是否保留？需确认上游 integrated_recommendation 是否有该字段

#### P0-3: 风控检查补齐
**问题**: Trading 缺少单股仓位上限检查和行业集中度检查
**方案**:
- Trading: 补齐单股仓位上限（需对比现有持仓 + 新买入 vs max_position_ratio）
- Trading + Backtest: 补齐行业集中度检查（industry_code 集中度 vs max_industry_ratio）
**修改文件**: `src/trading/pipeline.py`, `src/backtest/pipeline.py`

#### P0-4: 信号字段读取补齐
**问题**: Trading 只读10个字段，设计要求18个
**方案**: 按实际需要分批补齐
- 立即需要: `stop`, `target`（止损止盈计算前置条件）, `neutrality`（v2.0验证需要）
- 可缓: `stock_name`, `mss_score`, `irs_score`, `pas_score`（追溯用，非执行必须）
**修改文件**: `src/trading/pipeline.py` — `_read_signals()`

#### P0-5: Backtest 补齐总仓位上限
**问题**: Trading 有 max_total_position 检查但 Backtest 没有
**方案**: Backtest 买入循环中增加总仓位占比检查
**修改文件**: `src/backtest/pipeline.py`

---

### 🟠 第二批 P1（重要 — 影响数据完整性和风控深度）

#### P1-1: trade_records 字段对齐
**方案**: Trading 的 TRADE_RECORD_COLUMNS 补齐缺失的关键字段:
- 必补: `slippage`, `fill_probability`, `fill_ratio`, `liquidity_tier`, `impact_cost_bps`, `signal_id`, `trading_state`
- 可缓: `stock_name`, `execution_mode`, `slice_seq`, `updated_at`
- 代码独有的 `t1_restriction_hit`, `limit_guard_result`, `session_guard_result`, `risk_reward_ratio`, `contract_version` → 纳入设计文档

#### P1-2: positions 字段对齐
**方案**:
- 必补: `cost_amount`, `unrealized_pnl`, `unrealized_pnl_pct`, `signal_id`, `stop_price`, `target_price`
- 设计决策: positions 存储模型统一为"按 trade_date 分快照"（代码方案），设计中的 `stock_code UNIQUE` 方案作废
- 代码独有的 `trade_date`, `contract_version` → 纳入设计文档

#### P1-3: 费用计算统一
**方案**: 确定统一标准后 Trading 和 Backtest 复用同一计算函数
- 提取: `src/shared/fee_calculator.py`
- 设计侧决策: 费用分档(S/M/L)是否保留？冲击成本是否纳入 Trading?
- 若保留分档: 设计文档需补充费用分档定义
- 若不保留: Backtest 回退到标准费率

#### P1-4: 信号质量验证 v2.0 实现
**方案**: 根据设计实现 ValidationResult 逻辑
- 前置条件: P0-4 先补齐 neutrality 字段读取
- 实现: 在 Trading 买入流程中增加 position_adjustment 仓位缩减
- 设计侧决策: Backtest 是否也需要 v2.0 验证？（设计未明确）

#### P1-5: Gate 机制文档同步
**方案**: 设计文档更新为反映代码的双重门禁（backtest_results + quality_gate_report）
**修改文件**: `trading-algorithm.md` §2.1

---

### 🟡 第三批 P2（次要 — 数据规范性和可维护性）

#### P2-1: 设计文档实现状态更新
3个文档(`trading-algorithm.md`, `trading-data-models.md`, `trading-information-flow.md`)的「实现状态」章节需全部更新为反映 Pipeline 模式已落地的现状。

#### P2-2: risk_events 表纳入设计
代码中 risk_events 已被大量使用，需在 `trading-data-models.md` 中补充 DDL 定义。

#### P2-3: t1_frozen 设计vs代码对齐
设计要求独立表，代码用 can_sell_date 内联。
方案: 设计文档同步为 can_sell_date 方案（代码方案更简洁、已通过验证），标记 t1_frozen 独立表为废弃。

#### P2-4: RejectReason 枚举同步
- 设计补充: `REJECT_NO_MARKET_PRICE`, `REJECT_ONE_WORD_BOARD`, `REJECT_LIQUIDITY_DRYUP`
- 设计废弃/标注: `REJECT_NO_OPEN_PRICE` → 改为 `REJECT_NO_MARKET_PRICE`

#### P2-5: Backtest 超额实现纳入设计
一字板检测、流动性枯竭检测、冲击成本乘数 → 补入 `trading-algorithm.md` 作为正式规则

#### P2-6: fill_ratio 公式修正
Backtest 的 fill_ratio 缺少 capacity_ratio 变量，需对齐设计公式:
```python
# 当前: fill_ratio = 1.0 - 0.5 * queue_ratio
# 应为: fill_ratio = 1.0 - 0.5 * queue_ratio - 0.5 * capacity_ratio
```

#### P2-7: 流动性分层从绝对阈值改为百分位
设计用 p70/p30 百分位，Backtest 用绝对阈值 100万/20万。
决策: 若暂不改代码，设计文档需同步为绝对阈值方案。

---

### 🔵 第四批 P3（远期 — 架构演进）

#### P3-1: 止损/止盈/最大回撤实现
设计中有完整的止损检查(§3.4)、最大回撤检查(§3.5)，但两套代码都是"T+1 无条件平仓"。
这是一个根本性的策略差异，需要单独评估：
- 当前"日内交替"模式是有意为之还是简化实现？
- 如需实现多日持仓策略，Trading/Backtest 都需要大规模重构

#### P3-2: Regime 阈值解析
设计中 `resolve_regime_thresholds()` 依赖 MSS temperature 和 market_volatility_20d 动态调节风控参数。
需在上游数据可用后实现。

#### P3-3: 订单状态机完善
当前仅 filled/rejected 两态。
是否需要 pending/submitted/partially_filled/cancelled 取决于是否对接真实交易所。
纸上交易阶段可暂不扩展。

#### P3-4: 枚举类定义
代码中全部使用硬编码字符串，可逐步替换为枚举类以增强类型安全。

---

## 四、急救执行顺序建议

```
Phase 1 (立即):
  P0-2 信号过滤统一  →  确保 Trading/Backtest 消费同一信号集
  P0-3 风控检查补齐  →  消除最大的风控缺口
  P0-5 Backtest补齐总仓位上限

Phase 2 (紧随):
  P0-1 成交模型对齐  →  Trading 复用 Backtest 的部分成交逻辑
  P0-4 信号字段补齐  →  为 v2.0 验证做准备
  P1-3 费用计算统一  →  提取共享模块

Phase 3 (本周):
  P1-1 trade_records 字段对齐
  P1-2 positions 字段对齐
  P2-1 设计文档实现状态更新
  P2-2 risk_events 纳入设计
  P2-3 t1_frozen 对齐

Phase 4 (下周):
  P1-4 信号质量 v2.0
  P1-5 Gate 机制同步
  P2-4~P2-7 枚举/公式/分层修正

Phase 5 (远期):
  P3-1~P3-4 止损/regime/状态机/枚举类
```

---

## 五、需要您做的决策

在开始急救之前，以下决策点需要您确认：

1. **整体方向**: 选择方向A/B/C？（推荐C：双向收敛）
2. **持仓策略**: 当前"T+1 无条件全卖"是临时简化还是正式策略？如果是简化，P3-1 需提前到 Phase 2
3. **费用分档**: Backtest 独创的 S/M/L 费用分档，保留还是去掉？
4. **opportunity_grade**: 上游 integrated_recommendation 是否有该字段？是否保留 D 级过滤？
5. **Backtest 是否需要 v2.0 验证**: 设计未明确，需确定 Backtest 是否也走 neutrality-based 仓位调整
6. **执行节奏**: 是否按上述 Phase 1-5 顺序推进，还是有优先级调整？
