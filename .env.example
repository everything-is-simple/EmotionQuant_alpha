# EmotionQuant 环境变量配置模板
# 复制此文件为 .env 并填入实际值
# cp .env.example .env

# ========================================
# TuShare API 配置
# ========================================
# TuShare API Token（需要 5000+ 积分）
# 获取地址：https://tushare.pro/user/token
TUSHARE_TOKEN=

# TuShare API 限流（每分钟调用次数）
TUSHARE_RATE_LIMIT_PER_MIN=120

# ========================================
# 数据路径配置
# ========================================
# 数据根目录（存放 Parquet、缓存、日志等）
# 实际部署路径：G:\EmotionQuant_data（仓库外独立目录，与代码分离）
# 若留空则回退到 ~/.emotionquant/data
DATA_PATH=G:\EmotionQuant_data

# DuckDB 目录（与 docs/system-overview.md 对齐：默认单库 emotionquant.duckdb；达到阈值后可启用分库）
# 若留空则默认 ${DATA_PATH}/duckdb
DUCKDB_DIR=

# Parquet 数据目录（可选，默认使用 ${DATA_PATH}/parquet）
PARQUET_PATH=

# 缓存目录（可选）
CACHE_PATH=

# 日志目录（可选）
LOG_PATH=

# ========================================
# 系统配置
# ========================================
# 日志级别：DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 运行环境：development, production
ENVIRONMENT=development

# Streamlit 前端端口
STREAMLIT_PORT=8501

# 回测配置
BACKTEST_INITIAL_CAPITAL=1000000
BACKTEST_COMMISSION_RATE=0.0003
BACKTEST_STAMP_DUTY_RATE=0.001
BACKTEST_TRANSFER_FEE_RATE=0.00002
BACKTEST_MIN_COMMISSION=5.0
