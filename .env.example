# EmotionQuant 环境变量配置模板
# 复制此文件为 .env 并填入实际值
# Linux/macOS: cp .env.example .env
# Windows PowerShell: Copy-Item .env.example .env

# ========================================
# TuShare API 配置
# ========================================
# 主路（推荐放第三方 10000 积分试用号；可频繁替换）
# 只改这一组即可切换主路账号
TUSHARE_PRIMARY_TOKEN=

TUSHARE_PRIMARY_SDK_PROVIDER=tinyshare
# 可选：第三方网关地址（示例：http://106.54.191.157:5000）
TUSHARE_PRIMARY_HTTP_URL=

# 兜底（推荐放官方 5000 积分号）
TUSHARE_FALLBACK_TOKEN=

TUSHARE_FALLBACK_SDK_PROVIDER=tushare
# 可选：兜底通道网关地址（通常留空走官方默认）
TUSHARE_FALLBACK_HTTP_URL=

# 兼容旧字段（可留空；若填写，将作为主路配置）
TUSHARE_TOKEN=
TUSHARE_SDK_PROVIDER=tushare
# 兼容旧字段：等价于 TUSHARE_PRIMARY_HTTP_URL
TUSHARE_HTTP_URL=

# TuShare API 限流（每分钟调用次数）
TUSHARE_RATE_LIMIT_PER_MIN=120

# ========================================
# 数据路径配置
# ========================================
# 数据根目录（存放 Parquet、缓存、日志等）
# 实际部署建议：仓库外独立目录（如 G:\EmotionQuant_data 或 /data/emotionquant）
# 若留空则回退到 ~/.emotionquant/data
DATA_PATH=

# DuckDB 目录（与 docs/system-overview.md 对齐：默认单库 emotionquant.duckdb；达到阈值后可启用分库）
# 若留空则默认 ${DATA_PATH}/duckdb
DUCKDB_DIR=

# Parquet 数据目录（可选，默认使用 ${DATA_PATH}/parquet）
PARQUET_PATH=

# 缓存目录（可选）
CACHE_PATH=

# 日志目录（可选）
LOG_PATH=

# ========================================
# 系统配置
# ========================================
# 仓库远端地址（统一入口，避免在多个文档中硬编码）
# 修改远端时，优先只改这里
REPO_REMOTE_URL=https://github.com/everything-is-simple/EmotionQuant_alpha
# 仓库备份远端地址
REPO_BACKUP_REMOTE_URL=https://gitee.com/wangweiyun2233/emotion-quant-alpha

# 日志级别：DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 运行环境：development, production
ENVIRONMENT=development

# Streamlit 前端端口
STREAMLIT_PORT=8501

# 回测配置（建议优先使用 BACKTEST_INITIAL_CASH）
BACKTEST_INITIAL_CASH=1000000
# 兼容旧字段，若只设置旧字段也可工作（建议后续迁移到 BACKTEST_INITIAL_CASH）
BACKTEST_INITIAL_CAPITAL=1000000
BACKTEST_COMMISSION_RATE=0.0003
BACKTEST_STAMP_DUTY_RATE=0.001
BACKTEST_TRANSFER_FEE_RATE=0.00002
BACKTEST_MIN_COMMISSION=5.0
BACKTEST_RISK_FREE_RATE=0.015
BACKTEST_SLIPPAGE_VALUE=0.001
BACKTEST_MAX_POSITIONS=10
BACKTEST_MAX_POSITION_PCT=0.20
BACKTEST_STOP_LOSS_PCT=0.08
BACKTEST_TAKE_PROFIT_PCT=0.15
BACKTEST_MIN_FINAL_SCORE=55.0
BACKTEST_TOP_N=20
BACKTEST_MAX_HOLDING_DAYS=10
